[position]
# airfield = "EDRK"

[clouds]
cloud_minimum = 6
cloud_marginal = 15

[temperature]
temp_minimum = 0
spread_minimum = 3

[wind]
wind_var_maximum = 45
wind_maximum = 15
gust_maximum = 10

[age]
age_maximum = 21600
age_marginal = 3600

[visibility]
visibility_minimum = 1500
visibility_marginal = 5000

[taf]
# Maximum age for TAF forecasts in seconds (default: 86400 = 24h)
taf_age_maximum = 86400
# Marginal age for TAF forecasts in seconds (default: 21600 = 6h)
taf_age_marginal = 21600
# Highlight PROB groups in bright red (default: true)
taf_highlight_probability = true
# Show change group time windows (default: true)
taf_show_change_times = true
